{
  "name": "strata",
  "version": "0.1.0",
  "description": "Strata site reliability validation console for incident telemetry, replay, and remediation simulation.",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "npm run check",
    "check": "node --check src/server.js && node --check src/lib/incidents.js && node --check src/lib/sampleData.js && node --check public/app.js && python3 -m py_compile scripts/traffic_generator.py"
  },
  "keywords": [
    "aiops",
    "devops",
    "logs",
    "portfolio"
  ],
  "dependencies": {
    "express": "^4.22.1",
    "pino": "^9.14.0",
    "pino-http": "^10.5.0"
  },
  "license": "MIT"
}
